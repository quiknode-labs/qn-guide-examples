# Quicknode API Key (from Quicknode dashboard)
QN_API_KEY=""

# Stream Security Tokens (from Stream creation response)
QN_STREAM_SECURITY_TOKEN_EVM=""
QN_STREAM_SECURITY_TOKEN_SOL=""

# Quicknode EVM Endpoint URL (for ENS resolution and ERC-20 metadata)
QN_EVM_ENDPOINT=""

# Solana RPC Endpoint URL (for SPL metadata/decimals)
QN_SOLANA_ENDPOINT=""

# Database URL (SQLite)
DATABASE_URL="file:./dev.db"

# App URL (for webhook destination - use ngrok for local dev)
APP_URL="http://localhost:3000"
